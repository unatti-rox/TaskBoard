/*
  Shared team mode. Leave both values empty to run as a single-browser demo.
  To connect Supabase, copy these from your Supabase project:
  Project Settings → API → Project URL and the "anon public" key.
  The anon key is meant to be public; the database's row level security protects the data.
*/
window.TASKBOARD_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: ""
};
